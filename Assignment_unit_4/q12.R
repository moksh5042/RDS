# 11. Create a nested list
nested_list <- list(
  simple_value = 100,
  complex_data = list(
    numbers = c(1, 2, 3, 4, 5),
    letters = c("a", "b", "c")
  )
)

# 12. Extract second element from nested list
second_nested <- nested_list[[2]]  # Gets the "complex_data" list

